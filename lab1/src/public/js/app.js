(async () => {
    try {
        // const resData = await fetch('http://localhost:3001/vnPlate');
        // const data = await resData.json();
        // const template = `
        // ${data.map((item, index) => `<option value='${index}'>${item.city}</option>`).join('')}
        // `;
        // document.querySelector('#checkPlate').innerHTML += template;
    } catch (error) {}
})();
